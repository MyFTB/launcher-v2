version: 2

updates:
  # ── npm ────────────────────────────────────────────────────────────────────
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "Europe/Berlin"
    # Keep PR count manageable by grouping related packages
    groups:
      electron:
        patterns:
          - "electron"
          - "electron-*"
      react:
        patterns:
          - "react"
          - "react-*"
          - "@types/react*"
      vite:
        patterns:
          - "vite"
          - "vite-*"
          - "@vitejs/*"
          - "electron-vite"
      tailwind:
        patterns:
          - "tailwindcss"
          - "@tailwindcss/*"
      typescript-tooling:
        patterns:
          - "typescript"
          - "typescript-eslint"
          - "eslint"
          - "globals"
          - "@types/node"
      xmcl:
        patterns:
          - "@xmcl/*"
    open-pull-requests-limit: 10
    # Ignore major bumps on packages with known migration cost
    ignore:
      - dependency-name: "electron-vite"
        update-types: ["version-update:semver-major"]
      - dependency-name: "@vitejs/plugin-react"
        update-types: ["version-update:semver-major"]

  # ── GitHub Actions ────────────────────────────────────────────────────────
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    groups:
      github-actions:
        patterns:
          - "*"
    open-pull-requests-limit: 5
